# MiniMax Code API 代理配置
# 在管理界面中修改，不要手动编辑

target:
  base_url: ""    # 目标 API Base URL
  api_key: ""     # 目标 API Key
  name: "custom"  # 配置名称
  enabled: false  # 默认关闭（使用原版 MiniMax API）

minimax:
  # 安装目录会自动检测，如果检测失败可手动填写
  install_dir: ""
  daemon_js: "resources/resources/daemon/daemon.js"
